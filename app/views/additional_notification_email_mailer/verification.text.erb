Hello <%= @user.nickname.presence || @user.first_name %>,

Use this link to verify that you own the additional email address requested in <%= @product_name %>:

<%= @verification_url %>

The link expires after 24 hours. If you did not request this, you can ignore this message. No notification copies are sent to this address until verification succeeds.
