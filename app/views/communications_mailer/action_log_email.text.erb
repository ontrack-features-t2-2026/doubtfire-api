Hi <%= @recipient&.nickname.presence || @recipient&.first_name || @recipient&.name %>,

RESULT
<%= @affected_students_count %> student<%= "s" unless @affected_students_count == 1 %> matched this rule and were affected by the configured actions.

<% @body_paragraphs.each do |paragraph| %>
<%= paragraph %>

<% end %>
A full log of actions is attached as a CSV.

Cheers,
The <%= @doubtfire_product_name %> Team

---

Generated with <%= @doubtfire_product_name %>
